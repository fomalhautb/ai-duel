# Agent Instructions

- 项目尚未上线，不需要任何向后兼容：schema、协议、存档、API 都可以直接做破坏性修改，不保留迁移路径和兼容层。（上线需要兼容时会移除本条。）
- 能不自己造轮子就不造轮子：动手实现任何非业务核心的功能前，先上网搜索是否已有现成的解决方案。
- 优先采用成熟的第三方库或开源外部服务，而不是自己从头实现；只有确认现有方案确实无法满足需求时才考虑自建。
- 除非另有指定，PR（标题和描述）、UI 文案、代码注释、文档以及与用户的沟通一律使用中文；代码标识符保持英文。
- 尽量少直接控制浏览器，因为浏览器交互会消耗大量 token。优先通过代码检查、自动化测试或命令行工具完成验证。
- 写代码注释前先检查相关调用方，确认被注释声明在系统中的实际用途和约束。
- 注释应解释容易令人疑惑的设计原因、意义、边界条件或意外行为，不要只是复述代码做了什么。
- 注释尽量使用简单易懂的语言，避免不必要的术语、复杂句式和含糊表达。
- 修改代码时同步检查并更新相关注释和文档，避免说明与实际行为不一致。
- 使用 Fable 5 系列模型时，主 agent（Fable 5）只负责计划、设计和验收；所有写代码的工作都派给 Opus 5 子 agent 完成，以减少主 agent 的 token 消耗。
